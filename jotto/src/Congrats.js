/**
 * Functional react component for congratulatory message.
 * @function
 * @param {object} props
 * @returns {JSX.Element}
 */
const Congrats = ({ success }) => {
  return <div />;
};

export default Congrats;
